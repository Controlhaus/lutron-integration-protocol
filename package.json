{
  "name": "global-cache-contact-closures",
  "version": "0.1.0",
  "description": "A catch connect service to control Global Cache contact closures.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Patrick Murray",
  "license": "MIT",
  "dependencies": {
    "yargs": "^12.0.5"
  }
}
