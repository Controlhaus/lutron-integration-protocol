{
  "name": "global-cache-contact-closures",
  "version": "0.1.0",
  "description": "Controls Global Cache iTach Contact Closures via TCP.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Patrick Murray",
  "license": "MIT",
  "dependencies": {
    "yargs": "^12.0.5"
  }
}
